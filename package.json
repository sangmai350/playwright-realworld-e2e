{
  "name": "playwright-realworld-pilot",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  }
}